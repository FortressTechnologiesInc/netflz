name: SonarQube Scan

on:
  push:
    branches:
      - main

jobs:
  sonarqube:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v2

    - name: Set up Java
      uses: actions/setup-java@v2
      with:
        java-version: '11'

    - name: SonarQube Scan
      run: |
        sonar-scanner \
          -Dsonar.projectKey=netflz \
          -Dsonar.sources=. \
          -Dsonar.host.url=http://192.168.0.17:9000 \
          -Dsonar.login=squ_fcd51069e8f0f1e4838f37df32fd564c2cd4a860
